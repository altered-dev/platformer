product: macos/app

apply:
  - ../../platformer.module-template.yaml

dependencies:
  - ../../shared

dependencies@macosArm64:
  - $libs.skiko.macos.arm64

dependencies@macosX64:
  - $libs.skiko.macos.x64
