product: android/app

apply:
  - ../../platformer.module-template.yaml

dependencies@android:
  - ../../shared
  - $libs.skiko.android: compile-only
  - $libs.skiko.android.arm64: runtime-only
  - $libs.skiko.android.x64: runtime-only
