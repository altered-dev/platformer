product:
  type: lib
  platforms: [jvm, android, macosArm64, macosX64, iosArm64, iosSimulatorArm64, iosX64]

apply:
  - ../platformer.module-template.yaml

aliases:
  - desktop: [jvm, macos]
  - mobile: [android, ios]

dependencies:
  - ../koml: exported
  - $libs.skiko: exported
  - $libs.kotlinx.serialization.cbor: exported

settings:
  kotlin:
    serialization: json
