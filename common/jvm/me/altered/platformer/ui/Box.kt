package me.altered.platformer.ui

import org.jetbrains.skia.Canvas

class Box(
    width: Size = expand,
    height: Size = expand,
    margin: Insets = none,
) : WidgetGroup(width, height, margin) {

    override fun draw(canvas: Canvas) {

    }
}
